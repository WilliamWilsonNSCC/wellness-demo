# Prevents npm from running lifecycle scripts during install (preinstall, install, postinstall, etc.)
ignore-scripts=true
# Blocks packages with suspicious subdependencies to prevent dependency attacks
block-exotic-subdeps=true
# Requires packages to be at least 24 hours old before installation
minimum-release-age=1440
# Only allows package upgrades, prevents downgrades to vulnerable versions
trust-policy=no-downgrade

# <h1 style={{ marginBottom: 4 }}>Welcome back{user?.name ? `, ${user.name.split(' ')[0]}` : ''}!</h1>
#             <p style={{ color: '#555', marginTop: 0 }}>Here's your wellness snapshot.</p>
        
#             {!summary ? (
#                 <div style={{ background: 'white', borderRadius: 12, padding: '2rem', textAlign: 'center', boxShadow: '0 2px 12px rgba(0,0,0,0.06)' }}>
#                 <p>No entries yet — head to the <a href="/tracker">Tracker</a> to log your first one!</p>
#                 </div>
#                 ) : (
#                 <>
#                 {/* ── Summary cards ──────────────────────────────────────────────── */}
#                 <p style={{ fontSize: '0.9rem', color: '#777', marginBottom: '0.5rem' }}>
#                     Averages · {summary.periodLabel} · {summary.entryCount} {summary.entryCount === 1 ? 'entry' : 'entries'}
#                 </p>
#                 <div style={{ display: 'flex', gap: '1rem', flexWrap: 'wrap', marginBottom: '2rem' }}>
#                     <StatCard label="Mood"       value={summary.mood} />
#                     <StatCard label="Stress"     value={summary.stress} />
#                     <StatCard label="Energy"     value={summary.energy} />
#                     <StatCard label="Sleep"      value={summary.sleep} />
#                     <StatCard label="Motivation" value={summary.motivation} />
#                 </div>
        
#                 {/* ── Latest journal ─────────────────────────────────────────────── */}
#                 {summary.latestJournal && (
#                     <div style={{ background: 'white', borderRadius: 12, padding: '1.5rem', boxShadow: '0 2px 12px rgba(0,0,0,0.06)', marginBottom: '2rem' }}>
#                     <h3 style={{ margin: '0 0 0.5rem' }}>Latest journal entry</h3>
#                     <p style={{ color: '#444', margin: 0 }}>{summary.latestJournal}</p>
#                     <p style={{ fontSize: '0.8rem', color: '#999', marginTop: 8 }}>
#                         {new Date(summary.latestDate).toLocaleDateString(undefined, { weekday: 'long', month: 'long', day: 'numeric' })}
#                     </p>
#                     </div>
#                 )}
        
#                 {/* ── Recent entries list ────────────────────────────────────────── */}
#                 {entries.length > 0 && (
#                     <div>
#                     <h3>Recent entries</h3>
#                     {entries.map((e) => (
#                         <div key={e.id} style={{
#                         background: 'white', borderRadius: 10, padding: '1rem 1.2rem',
#                         marginBottom: '0.75rem', boxShadow: '0 1px 6px rgba(0,0,0,0.05)',
#                         display: 'flex', justifyContent: 'space-between', alignItems: 'center', flexWrap: 'wrap', gap: '0.5rem'
#                         }}>
#                         <span style={{ fontSize: '0.85rem', color: '#777' }}>
#                             {new Date(e.date).toLocaleDateString(undefined, { month: 'short', day: 'numeric' })}
#                         </span>
#                         <span>😊 {e.mood} · 😓 {e.stress} · ⚡ {e.energy} · 😴 {e.sleep} · 🎯 {e.motivation}</span>
#                         {e.journal && <span style={{ fontSize: '0.85rem', color: '#555', fontStyle: 'italic', width: '100%' }}>"{e.journal.slice(0, 80)}{e.journal.length > 80 ? '…' : ''}"</span>}
#                         </div>
#                     ))}
#                     </div>
#                 )}
#                 </>
#             )}